import { Button } from "@mui/material";
import { Box } from "@mui/material";

export default function Navbar() {
  return (
    <Box display="flex">
      <Button>Home</Button>
      <Button>About</Button>
    </Box>
  );
}
