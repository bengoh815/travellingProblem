export type User = {
  id: number;
  firstName: string;
  lastName: string;
  email: string;
  password: string;
  isAdmin: boolean;
};

const Profile = () => {
  return <div>This is profile page </div>;
};

export default Profile;
